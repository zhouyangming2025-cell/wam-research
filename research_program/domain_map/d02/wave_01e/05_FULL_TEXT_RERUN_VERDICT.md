# D02-W1E Full-Text Rerun Verdict

## Verdict

**FULL-TEXT EVIDENCE UPGRADED — HOLD-STRUCTURAL-PRESSURE**

The evidence rerun is complete. The full local MinerU text materially improves confidence and resolves the HUGSIM evidence gap. It also catches a meaningful BeTop boundary that the abstract/shallow pass could not expose.

## Statistics

| Check | Result |
|---|---:|
| Cases reaudited | 7 |
| `KEEP` | 5 |
| `REMAP` | 2 |
| `REMAIN-UNKNOWN` | 0 |
| Full raw Markdown used | 7/7 |
| New route axis | 0 |
| Deleted route axis | 0 |
| New R/W/E code | 0 |
| Existing ontology files modified | 0 |
| False collision established | 0 |
| Structural pressure cases | 1 (P0004 BeTop) |

## What became more credible

- TOAD is confirmed as candidate search with a learned utility scorer and no online world carrier.
- Hydra-MDP is confirmed as candidate selection using imitation plus decomposed simulation-derived outcomes.
- GenAD is confirmed as direct joint world/action emission without a downstream resolver.
- HUGSIM is no longer `UNKNOWN`; it is a simulator/world-generation boundary with externally supplied planner control.
- Vista's reward remains an evaluation capability, not a proven final action resolver.
- VAD's predicted future-agent motion is sufficiently documented to retain its `W4` route without turning internal non-ego mode choice into `RB`.

## What must be decided by a human

BeTop's full text demonstrates `RB + W4 + ED`, but its future-horizon prediction appears to be shared while the ego candidates are resolved. The existing rule requiring `X3` for `RB + W4` may be too narrow if shared predicted futures can legitimately serve as a resolver criterion.

This is a constraint-level audit question, not evidence for a new axis or a new human route code. The next action should be a focused human decision on whether to:

```text
KEEP the current constraint and classify BeTop's carrier differently;
CLARIFY the constraint to allow shared W4 at a candidate resolver; or
REQUIRE code evidence before deciding.
```

No ontology amendment is applied in this rerun.

## Preservation

The prior Wave 1 result, the 12-paper ontology, and the Safe-Sim acquisition record remain unchanged. P0066 Safe-Sim is not retroactively added to the sealed blind set; it is available for a later non-blind application test.
