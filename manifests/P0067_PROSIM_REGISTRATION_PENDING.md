# P0067 ProSim — Manifest Registration Pending

Last updated: 2026-09-19

The canonical paper, official project and official code are verified and recorded in:

```text
papers/raw_md/P0067_ProSim/P0067_ProSim.source_note.md
```

`manifests/CORPUS_MANIFEST.csv` was fetched at the current blob SHA
`8f3b17ad62e0e7dcfdf2e9cbb96fe214dda4ca09`, but its bytes are not UTF-8 text (the working copy is detected as binary). The GitHub text connector can read it only as base64 and cannot safely perform a CSV text replacement. No manifest bytes were changed. Local reconciliation is required before adding the P0067 row.

This note is an explicit pending status, not a second corpus entry.
